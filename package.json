{
    "name": "aisthea-project",
    "version": "1.0.0",
    "private": true,
    "scripts": {
        "dev": "concurrently \"npm run dev --prefix server\" \"npm run dev --prefix client\"",
        "kill:port": "powershell.exe -Command \"$p = (Get-NetTCPConnection -LocalPort 5000 -State Listen -ErrorAction SilentlyContinue).OwningProcess; if ($p) { Stop-Process -Id $p -Force; Write-Host 'Killed PID' $p } else { Write-Host 'Port 5000 is free' }\"",
        "dev:safe": "npm run kill:port && npm run dev",
        "install:all": "npm install && npm install --prefix server && npm install --prefix client",
        "postinstall": "npm install --prefix server && npm install --prefix client",
        "start": "npm run dev",
        "i18n:check": "node scripts/i18n-checker.mjs",
        "i18n:check:update-baseline": "node scripts/i18n-checker.mjs --update-baseline"
    },
    "devDependencies": {
        "@playwright/test": "^1.58.2",
        "@types/jest": "^30.0.0",
        "concurrently": "^9.2.1",
        "dotenv": "^17.3.1"
    }
}
