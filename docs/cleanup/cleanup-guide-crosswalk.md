# Cleanup Guide Crosswalk

Last reviewed: 2026-03-25
Guide source: `AISTHEA_FULL_PROJECT_CLEANUP_GUIDE_EN.md` in NotebookLM (`AISTHEA` notebook)

This file maps each cleanup-guide phase to the current repository state so the remaining cleanup work can be closed deliberately before resuming implementation work.

| Phase | Status | Current repo state | Remaining closure work |
| --- | --- | --- | --- |
| A. Freeze and Baseline | `[Done]` | Worktree is clean, cleanup is again being treated as a dedicated pass, and recent cleanup-only commits were separated from feature work. | Keep implementation paused until the remaining open phases below are closed. |
| B. Root-Level Project Cleanup | `[Done]` | Root tracked files are limited to the active onboarding/architecture/progress/checklist docs plus `package.json` and `.gitignore`; `scripts/` currently contains only `i18n-checker.mjs` plus its baseline JSON, both still referenced by root scripts; local agent/tooling folders such as `.agent/`, `.claude/`, and `.codex/` are ignored rather than tracked repo clutter. | Keep any future root additions intentional and documented, but no further tracked root/docs/tooling cleanup is currently blocking closure. |
| C. Client Cleanup | `[Done]` | Client test placement is now predictable; legacy root folders `client/src/components` and `client/src/pages` were removed; misplaced files such as `tracking.store.ts` were moved out of UI folders; shared CSS was centralized under `client/src/common/styles/`; unused asset placeholders such as `client/src/assets/images/.gitkeep` and `client/src/assets/styles/.gitkeep` were removed; dead storefront/admin hook wrappers `client/src/common/hooks/useCategories.ts`, `client/src/common/hooks/useOrders.ts`, and `client/src/common/hooks/useReviews.ts` were removed after confirming they had no callers; suspicious-but-active helpers such as `groupVariantsHelper.ts` were reviewed and intentionally kept because renaming them would currently widen the blast radius into product/admin flows. | Keep future client cleanup opportunistic, but this phase is no longer blocking the guide. |
| D. Server Cleanup | `[Done]` | Server test placement looks consistent with `__tests__` folders; server structure is broadly readable; high-risk files remain intentionally untouched; the current hybrid server boundary is mapped in `docs/cleanup/server-structure-audit.md`; the dead duplicate subtree `server/src/utils/schemas/src/generated/` has been removed; the latest gate review also found no remaining empty directories or low-trust tracked server filenames worth a cleanup slice. | Keep future server cleanup opportunistic, but this phase is no longer blocking the guide. |
| E. Test Cleanup Across the Whole Project | `[Done]` | Client and server test placement conventions are now much more predictable, critical touched areas have continued to pass verification, and the focused placeholder-title scan no longer surfaces broad `render` / `works` / `test` style clutter beyond the already-fixed cases documented in `docs/cleanup/test-clutter-audit.md`. | Keep future test cleanup opportunistic, but this phase is no longer blocking the guide. |
| F. Naming Cleanup | `[Done]` | Several low-trust names were already removed indirectly via structure cleanup, and no obvious `Temp`/`New`/`v2` style tracked client test files remain. The narrow wrapper `validationUtils.ts` has now been renamed to `passwordValidation.ts`, while the remaining notable names have all been reviewed as intentional ownership cases: `groupVariantsHelper.ts` is still active in product/admin variant flows, `return-request.route-helpers.ts` still matches route-layer ownership, and `03_seed_data_standard_fixed.sql` is still called by the full reset SQL runner. | Keep future naming cleanup opportunistic, but this phase is no longer blocking the guide. |
| G. Import and Dependency Cleanup | `[Done]` | Imports were updated after each move; stale root legacy paths and dead barrels such as `store/components/index.ts` and `common/hooks/index.ts` were removed; no obvious reversed `common/* -> pages/*` dependency showed up in the latest quick scan; the latest findings now live in `docs/cleanup/import-dependency-audit.md`, including confirmation that the `@validation` alias and `server/src/shared/validation/index.ts` barrel are still active contract glue rather than dead wrappers, while orphaned hook wrappers `useCategories.ts`, `useOrders.ts`, and `useReviews.ts` plus the thin `review.validator.ts` wrapper have now been removed after caller verification. | Keep future dependency cleanup opportunistic, but this phase is no longer blocking the guide. |
| H. Progress and Understanding Recovery | `[Done]` | `AUDIT_PROGRESS.md` now references this crosswalk plus the server cleanup audit, and the remaining cleanup-guide gaps are explicitly mapped instead of being implied. | Keep it current as cleanup closes, but no additional recovery work is blocking the guide now. |
| I. Technical Verification Before Push | `[In Progress]` | Final Phase I verification has now been run: `npm run build --prefix client`, `npm run lint --prefix client`, `npm run build --prefix server`, `npx vitest run --pool=threads --poolOptions.threads.singleThread` in `client/`, and `npm test -- --runInBand` in `server/` all pass. The full client Vitest run also exposed four test-only ambiguity issues (`OrderActionPanel`, `RecentOrders`, `OrderTimeline`, `ReturnDetail`), which were fixed in this pass. The only red verification command left is `npm run i18n:check`, which currently reports 34 missing client keys, 505 new unused client keys vs baseline, and 0 new hardcoded active-route strings vs baseline. | Decide whether the current `i18n:check` findings must be resolved now or explicitly accepted as pre-existing non-cleanup debt before guide closure. After that decision, make the final cleanup-only closure commit. |

## Highest-Priority Open Gaps

1. Resolve or explicitly accept the current `i18n:check` debt so Phase I can close honestly.
2. If that blocker is cleared, make the explicit cleanup-guide closure commit.
