# Client Return/Refund Wording Audit

Date: 2026-03-25

Scope: initial checklist-driven audit slice for customer/admin return UI.

## Fixed in this slice

| File | Current text / behavior | Canonical text / behavior |
| --- | --- | --- |
| `client/src/store/pages/CreateReturnRequest.tsx` | Reason select looked up `reason.*` keys and could fall back to raw enums | Use `reasons.*` keys from `returns.json` |
| `client/src/store/pages/ReturnDetail.tsx` | Header mixed English with `Return #{{id}}` | Use canonical `Yêu cầu #{{id}}` detail title |
| `client/src/store/pages/ReturnDetail.tsx` | Error fallback used `return request` English phrase | Use `yêu cầu trả hàng` wording |
| `client/src/store/pages/ReturnDetail.tsx` | Attachment alt used `Attachment {{index}}` | Use `Ảnh minh chứng {{index}}` |
| `client/src/common/components/ReturnTimeline.tsx` | Timeline could show raw legacy aliases like `PENDING_APPROVAL` / `COMPLETED` | Normalize to canonical `REQUESTED` / `REFUNDED` before rendering |
| `client/src/admin/components/adminReturn.utils.ts` | Admin status label/tone helpers handled legacy aliases directly | Normalize aliases first, then render canonical status labels/tones |
| `client/src/store/pages/CreateReturnRequest.tsx` | Customer create page copy was mostly hardcoded in component | Move create-page labels, actions, and empty/error copy under `returns.json:create.*` |
| `client/src/store/pages/CreateReturnRequest.tsx` | Validation text said `Chọn ít nhất 1 item để trả` | Use canonical wording `Chọn ít nhất 1 sản phẩm để trả` |
| `client/src/admin/components/AdminReturnReviewModal.tsx` | First info card used the request-date label while displaying status | Use explicit status label |
| `client/src/common/components/StatusBadge.tsx` | Badge labels were hardcoded and did not normalize legacy aliases | Read canonical labels from `returns.status.*` after shared normalization |
| `client/src/common/services/return.service.ts` | Admin success/error messages were hardcoded in service layer | Return `messageKey` and let UI hooks/components resolve translated feedback |
| `client/src/admin/hooks/useAdminReturns.ts` | Toast handling depended mostly on pre-rendered message text | Resolve translated feedback from `messageKey` when available |
| `client/src/store/pages/CreateReturnRequest.tsx` | Schema-level validation copy was still inline in the component module | Build the create-form schema from translated validation strings |
| `client/src/common/services/return.service.ts` | Reject flow could still send a hardcoded Vietnamese fallback reason from the service layer | Require an explicit reject note and surface a keyed validation error instead |
| `client/src/admin/components/RecentOrders.tsx` | Dashboard recent-orders table depended on hardcoded status labels from `dashboard.service.ts` | Resolve recent-order labels from i18n in the component and keep the service export color-only |
| `client/src/common/pages/TrackingDetailPage.tsx` | Tracking page still hardcoded lookup-required/load-error copy and used a one-off `Return_Requested` sentinel internally | Resolve tracking error/redirect copy from `tracking.json`, switch tracking internals to canonical `RETURN_REQUESTED`, and lock rendering with tests |
| `client/src/common/pages/TrackingDetailPage.tsx` | Tracking detail page chrome and status labels could still expose raw `tracking.*` keys or canonical status codes when the namespace was unavailable | Resolve page-level tracking chrome and status labels through safe fallbacks so `RETURN_REQUESTED` / `RETURNED` states stay human-readable even in raw-key scenarios |
| `client/src/admin/components/adminReturn.utils.ts` | Shared admin return status helper could still expose raw `status.*` keys when the `returns` namespace was unavailable | Fall back to canonical Vietnamese status labels after normalization so admin return badges/details stay readable |
| `client/src/common/pages/OrderDetailPage.tsx` | Track-order CTA disappeared once an order moved into `RETURN_REQUESTED` or `Returned` states even though tracking UI handled those statuses | Keep the track CTA visible for return-requested and returned orders with regression tests |
| `client/src/admin/components/OrderStatusBadge.tsx` / `client/src/admin/components/OrderTimeline.tsx` / `client/src/admin/pages/OrderDetail.tsx` | Admin order status UI normalized unknown statuses through the order FSM and could fall back to pending-style meta for `RETURN_REQUESTED` | Add a local display helper for `RETURN_REQUESTED` so badge, timeline, and order detail keep orange return-requested styling and label fallback |
| `client/src/admin/components/OrderActionPanel.tsx` | Admin action panel derived next statuses only from the client FSM, so `RETURN_REQUESTED` lost the `Returned` action even though the server transition still exists | Add a local action-status resolver for `RETURN_REQUESTED` and lock the return-confirmation dialog flow with tests instead of widening the shared FSM |
| `client/src/common/components/OrderHeader.tsx` | Customer order header still used hardcoded chrome labels and could show raw translation keys when the page namespace was unavailable in tests/fallback scenarios | Move header chrome to `pages.orderDetail.header.*` and add safe label fallbacks so customer order detail keeps translated labels while still rendering `RETURN_REQUESTED` correctly |
| `client/src/common/components/OrderPricingSummary.tsx` | Customer pricing summary still hardcoded section chrome like subtotal, discount, and total labels | Move pricing-summary chrome to `pages.orderDetail.pricingSummary.*` and add safe label fallbacks so order detail keeps translated totals even when the page namespace is not loaded yet |
| `client/src/common/components/OrderItemsTable.tsx` | Customer items table still hardcoded section chrome like item count, SKU, and review CTA labels | Move order-items chrome to `pages.orderDetail.items.*` and add safe label fallbacks so delivered/returned orders keep translated labels without exposing raw i18n keys |
| `client/src/common/components/ShippingAddressCard.tsx` | Customer shipping-address section still hardcoded the section heading inside the card | Read the heading from `pages.orderDetail.shippingAddress` and keep a safe fallback label so order detail still renders correctly when the page namespace is unavailable |
| `client/src/common/pages/OrderDetailPage.tsx` | Customer order-detail hero, empty/error states, and action CTA labels could still fall back to raw i18n keys in tests or partial-loading scenarios | Resolve page-level copy from `pages.orderDetail.*` with safe fallback labels so the customer detail page stays readable while keeping canonical `RETURN_REQUESTED` behavior intact |
| `client/src/common/components/ReviewModal.tsx` | Customer review modal still hardcoded title, rating/comment labels, submit states, and review feedback copy | Move review-modal chrome to `pages.orderDetail.reviewModal.*` and add safe fallbacks so product-review flow stays readable even when the page namespace is unavailable |
| `client/src/store/pages/MyOrders.tsx` | Customer order-list hero, tabs, empty/loading states, and CTA labels could still fall back to raw `myOrders.*` keys in tests or partial-loading scenarios | Resolve `myOrders.*` page chrome through safe fallbacks so the customer order list stays readable while preserving the existing status/payment rendering |
| `client/src/common/pages/TrackingLookupPage.tsx` | Customer tracking-lookup hero, form labels, CTA, and validation/error states could still fall back to raw `trackingLookup.*` keys in tests or partial-loading scenarios | Resolve `trackingLookup.*` page chrome through safe fallbacks so the public tracking entrypoint stays readable before handing off to tracking detail |
| `client/src/common/pages/OrderSuccess.tsx` | Customer order-success confirmation, summary, and follow-up sections could still fall back to raw `orderSuccess.*` or `checkoutFlow.*` keys in tests or partial-loading scenarios | Resolve page-level `orderSuccess.*` copy with safe fallbacks and add explicit fallbacks for checkout progress labels so the post-checkout confirmation screen stays readable end to end |
| `client/src/common/pages/VNPayReturn.tsx` / `client/src/common/utils/paymentStatus.ts` | Customer VNPay return result screen and payment badge fallbacks could still expose raw `vnpayReturn.*` / `checkoutFlow.*` keys or ASCII fallback labels when namespaces were unavailable | Resolve `vnpayReturn.*` copy through safe fallbacks and switch payment-status fallback labels to canonical Vietnamese so post-payment states stay readable end to end |
| `client/src/admin/components/RefundDialog.tsx` | Admin refund dialog already used i18n keys but could still expose raw `refund.*` keys for headings, form chrome, CTA, and inline feedback when the orders namespace was unavailable in tests or partial-loading scenarios | Resolve refund-dialog chrome and direct feedback messages through safe fallbacks so the admin refund flow stays readable without changing the existing financial logic |
| `client/src/admin/components/OrderFinancials.tsx` | Admin refund-history card already used i18n keys but could still expose raw `refund.financials.*`, `refund.method.*`, or `refund.status.*` keys when the orders namespace was unavailable in tests or partial-loading scenarios | Resolve refund-history headings, table headers, method/status labels, and empty state through safe fallbacks so admin financial history stays readable without changing refund data behavior |
| `client/src/store/pages/ReturnDetail.tsx` | Return detail section labels and templated fallback copy could still expose raw `detail.*` keys when the `returns` namespace was unavailable | Resolve `detail.*` copy through safe fallbacks so the detail screen stays readable and still interpolates `id` / `date` / attachment labels correctly |
| `client/src/common/components/ReturnItemsTable.tsx` | Return item table headers, templated item fallback labels, and reason labels could still expose raw `itemsTable.*` / `reasons.*` keys when the `returns` namespace was unavailable | Resolve table chrome and templated fallbacks through readable Vietnamese defaults so the item summary stays human-readable |
| `client/src/common/components/ReasonLabel.tsx` | Shared reason labels could still render raw `reasons.*` keys when the `returns` namespace was unavailable | Fall back to the raw reason code instead of exposing the translation key |
| `client/src/admin/components/AdminReturnReviewModal.tsx` | Admin review modal headings, proof-image count copy, lightbox labels, and status headings could still expose raw `modal.*` keys when the `returns` namespace was unavailable | Resolve modal copy through safe fallbacks with template interpolation so the review flow stays readable without changing admin actions |
| `client/src/admin/pages/Returns.tsx` / `client/src/admin/hooks/useAdminReturns.ts` | Admin returns list chrome could still expose raw `returns.*` keys or unexpanded `{{count}}` / `{{page}}` placeholders when the namespace was unavailable | Resolve list chrome, tab labels, and feedback copy through safe fallbacks with template interpolation so the admin returns queue stays readable |
| `client/src/admin/pages/Tracking.tsx` | Admin tracking page chrome could still depend on the translation function returning `defaultValue` instead of raw keys in partial-loading scenarios | Resolve page chrome through safe fallbacks even when `pages.adminTracking.*` returns raw keys |
| `client/src/store/pages/SupportPage.tsx` | Customer support and return-policy copy could still depend on the translation function returning `defaultValue` instead of raw keys in partial-loading scenarios | Resolve `pages.support.*` copy through safe fallbacks so support/returns content stays readable |

## Remaining mismatches found

| File | Current text / behavior | Canonical text / behavior |
| --- | --- | --- |
| `Wider client return/refund audit` | Untouched screens may still contain wording drift outside the currently audited slice | Continue scanning remaining return/refund surfaces as future slices open |

## Notes

- This document is the mismatch list requested by the implementation checklist.
- The audit is started, but not yet final for every return/refund screen.
- The latest low-risk slices hardened `ReturnDetail`, `ReturnItemsTable`, `ReasonLabel`, and `AdminReturnReviewModal` against missing `returns` translations while keeping dynamic labels readable.
- `RecentOrders` now also keeps dashboard chrome and return-related status labels readable even when the `dashboard` / `orders` namespaces return raw keys.
- The admin `RefundDialog` and `OrderFinancials` surfaces now also interpolate templated fallback labels correctly even when `orders.refund.*` namespaces are unavailable.
- Shared `StatusBadge` and `ReturnTimeline` now also keep canonical status labels, legacy log comments, and actor/status helper copy readable even when the `returns` namespace is unavailable.
- `CreateReturnRequest` now also keeps form chrome, schema validation copy, header subtitle, item fallback labels, purchased-quantity text, and attachment alt text readable even when the `returns` namespace is unavailable.
- The admin `Returns` page plus `useAdminReturns` now also interpolate templated fallback labels correctly when the `returns` namespace returns raw keys, and the admin `Tracking` / customer `SupportPage` surfaces now explicitly cover the same raw-key fallback path.
- `ReviewModal`, `OrderDetailPage`, `OrderSuccess`, and `VNPayReturn` now also cover raw-key fallback paths that include templated copy such as star counts, buy-again counts, summary item counts, and order-id labels.
- `MyOrders` and `TrackingLookupPage` now also pin the raw-key fallback path explicitly in tests so those page-level labels stay readable even if the corresponding `pages.*` namespaces are only partially loaded.
- `TrackingDetailPage` now also keeps page chrome and return-related status labels readable when `tracking.*` returns raw keys, and `adminReturn.utils` now falls back to canonical Vietnamese labels when `returns.status.*` is unavailable.
- `OrderTimeline` and `TrackingTimeline` now also keep admin/store timeline empty-state copy readable when the `orders` / `tracking` namespaces return raw keys.
- `OrderDetail` now also keeps admin page chrome, metadata labels, section headings, guest-customer copy, and delivery-proof labels readable when `pages.adminOrderDetail.*` returns raw keys.
- `PaymentStatusBadge` and `PaymentMethodLabel` now also fall back to Vietnamese default labels when `enums.*` returns raw keys, so shared payment chips stay readable across order/payment screens.
- The next highest-value slice is scanning untouched return/refund surfaces for any remaining wording drift outside the currently audited files.
